# Node
node_modules/
.next/
dist/
build/
.env
.env.local

# .NET
bin/
obj/
*.user

# Editor / OS
.vscode/
.idea/
.DS_Store

# Claude Code (local/pessoal, não deve ir pro repo)
.claude/settings.local.json

# Material bruto de handoff/mídia (originais não otimizados, só as versões
# finais em assets/*.jpg|.mp4 são usadas pelo site — ver docs/ para contexto)
Portfolio site para Victoria.zip
prototype_extracted/
assets/landing page da Victoria/
assets/converted/
